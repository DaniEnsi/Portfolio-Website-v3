import * as LibraryInterface from "../interfaces/libraryInterface";
import Base from "../base";
import { UserPaginatedInput } from "../interfaces/shared";
export default class LibraryClass extends Base {
    getArtists(username: string, params?: {
        sk?: string;
        page?: number;
        limit?: number;
    }): Promise<LibraryInterface.getArtists>;
    getArtists(input: UserPaginatedInput): Promise<LibraryInterface.getArtists>;
}
