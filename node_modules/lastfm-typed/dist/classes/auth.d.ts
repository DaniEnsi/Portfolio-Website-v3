import * as AuthInterface from "../interfaces/authInterface";
import Base from "../base";
export default class AuthClass extends Base {
    getToken(): Promise<string>;
    getToken(input: {}): Promise<string>;
    getSession(token: string): Promise<AuthInterface.getSession>;
    getSession(input: {
        token: string;
    }): Promise<AuthInterface.getSession>;
    getMobileSession(username: string, password: string, token: string): Promise<AuthInterface.getSession>;
    getMobileSession(input: {
        username: string;
        password: string;
        token: string;
    }): Promise<AuthInterface.getSession>;
}
