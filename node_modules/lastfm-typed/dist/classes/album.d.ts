import * as AlbumInterface from "../interfaces/albumInterface";
import Base from "../base";
import { AlbumInput } from "../interfaces/shared";
export default class AlbumClass extends Base {
    addTags(artist: string, album: string, tags: string | string[], sk: string): Promise<{}>;
    addTags(params: AlbumInterface.addTagsInput): Promise<{}>;
    getInfo(album: AlbumInput, params?: {
        autocorrect?: boolean;
        username?: string;
        sk?: string;
        lang?: string;
    }): Promise<AlbumInterface.getInfo>;
    getInfo(params: AlbumInterface.getInfoInput): Promise<AlbumInterface.getInfo>;
    getTags(album: AlbumInput, username: string, params?: {
        sk?: string;
        autocorrect?: boolean;
    }): Promise<AlbumInterface.getTags>;
    getTags(params: AlbumInterface.getTagsInput): Promise<AlbumInterface.getTags>;
    getTopTags(album: AlbumInput, params?: {
        autocorrect?: boolean;
    }): Promise<AlbumInterface.getTopTags>;
    getTopTags(params: AlbumInterface.BasicAlbumInput): Promise<AlbumInterface.getTopTags>;
    removeTag(artist: string, album: string, tag: string, sk: string): Promise<{}>;
    removeTag(params: AlbumInterface.removeTagInput): Promise<{}>;
    search(album: string, params?: {
        limit?: number;
        page?: number;
    }): Promise<AlbumInterface.search>;
    search(params: AlbumInterface.searchInput): Promise<AlbumInterface.search>;
}
